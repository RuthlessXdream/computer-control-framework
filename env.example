# Computer Control Framework - 环境变量配置示例
# 
# 使用方式:
#   1. 复制此文件为 .env
#   2. 根据你的环境修改配置
#   3. source .env 或使用 python-dotenv 加载

# ==================== 日志配置 ====================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
CCF_LOG_LEVEL=INFO

# 日志文件目录 (默认: ./logs)
CCF_LOG_DIR=logs

# ==================== OmniParser 配置 ====================
# OmniParser 项目路径 (必须配置才能使用 OmniParser 检测)
OMNIPARSER_PATH=/path/to/OmniParser

# OmniParser 模型权重路径 (默认: $OMNIPARSER_PATH/weights)
# OMNIPARSER_WEIGHTS_PATH=/path/to/weights

# 检测阈值 (0.0 - 1.0, 默认: 0.05)
# OMNIPARSER_BOX_THRESHOLD=0.05
